import '@styles/globals.css';
export const metadata = {
    title: 'Call',
  }


  const CallLayout = ({children}) => {
    return (
      <section>
        {children}
      </section>
    )
  }
  
  export default CallLayout