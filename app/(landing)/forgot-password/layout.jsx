import '@styles/globals.css';
export const metadata = {
    title: 'Forgot Password',
    description: 'Recovers your password',
  }

const ForgotPasswordLayout = ({children}) => {
  return (
    <section>
      {children}
    </section>
  )
}

export default ForgotPasswordLayout
