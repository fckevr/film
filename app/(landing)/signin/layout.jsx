import '@styles/globals.css';
export const metadata = {
    title: 'Sign In',
    description: 'Sign In Page - Sex69',
  }

const SignInPage = ({children}) => {
  return (
    <section>
      {children}
    </section>
  )
}

export default SignInPage
