import '@styles/globals.css';
export const metadata = {
    title: 'Update Report',
  }


  const UpdateReport = ({children}) => {
    return (
      <section>
        {children}
      </section>
    )
  }
  
  export default UpdateReport
